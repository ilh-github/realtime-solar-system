# 2026-08-26 · 回归验证与当前日期 HORIZONS 对照

## 本轮范围

按 `OPENCODE_HANDOFF.md` 第 5 节执行回归基线、时间尺度与参考系验证、当前日期 HORIZONS 对照。全部验证通过浏览器（Playwright 驱动 Chrome）与控制台完成，未修改任何产品代码。用户未提交改动已按要求先检查后暂存。

## 回归基线

- `node --check app.js` / `sky.js` / `sw.js`：通过。
- `git diff --check`：通过。
- 工作区改动（首屏最小场景、太阳地心视轨线、三体验证接口、README 来源声明、默认开关调整）与交接文档"已完成"清单一致，已暂存未提交。
- `python3 -m http.server 4185`（新端口，无旧 Service Worker 缓存），页面加载零控制台错误（仅一条天象扫描正常日志）。

## 浏览器与控制台验证结果

### 首屏三体

- 首屏活动对象为 Earth（左侧面板高亮），太阳光晕、地球、月球同时可见；聚焦月球后本体、月相、轨道环渲染正常。
- J2000（`threeBodyResidualKm(2451545.0)`）：`earthMoonBarycenter = [0,0,0]`，`moonMinusEarth ≈ [-1.6e-9, -1.0e-8, 0] km`。
- 当前时刻（UTC JD 2461278.846）：两条残差同为机器精度量级；地日距离 151,184,038 km，月地距离 396,884 km。

### 时间尺度合同

- `parseDateToTtJd("2024-04-08T20:00",0)` 与 `parseDateToTtJd("2024-04-09T04:00",8)` 给出同一 TT 历元 `2460409.3341903`。
- `parseDateToTtJd("2000-01-01T12:00",0)` = `2451545.0007392806`，与预期 `2451545.00073928` 偏差 4.7e-10 天。

### 地心模式

- 切换"地心"后地球为原点，月球轨道环与太阳视轨线（右上橙色弧线）同时可见。
- 以"天/秒"播放 6 天：月球沿轨道正确推进，太阳视轨线保持连续，无跳变、无消失，符合逐时刻重采样行为（`geoTrace` 在 jd 变化 >0.125 天时重建）。

### sky.html UTC JD 合同（端到端）

1. 主页面内部 TT JD `2461298.0523`。
2. `ttJdToUtcJd` 转换（ΔT 75.51 s）→ 深链 `sky.html#lat=39.90&lon=116.40&jd=2461298.0514`。
3. sky.html 接收后显示 JD `2461298.05140`（UTC 语义），页面时间 `2026-09-14 21:14 CST` = UTC 13:14 + 8 h，链路一致。
4. 代码依据：`app.js:8052` 发送端 `ttJdToUtcJd(jd)`；`sky.js:296` 接收端 `state.jd` 按 UT 消费，内部 `jdTT()` 自行转 TT。

### 星历面板关闭行为

- 民用路径：输入 `2020-06-15T08:00` → 触发 input/change → 点击关闭 → 输入框保持 `2020-06-15T08:00`，面板关闭。
- 天文年路径：输入 `-0199-03-01 12:00` → 点击关闭 → 输入保持，提示文案正常。
- 关闭即提交（`applyDateInput()` → `syncDateInput(true)`），反写值与用户输入无损往返，未观察到覆盖。

## 2026-08-26 当前日期 HORIZONS/DE441 对照（本轮新增，此前网络失败）

- 对照方法与 `analysis-20260821-core-three-body.md` 相同：HORIZONS API DE441 几何向量，目标 399/中心 @sun、目标 301/中心 @399，`REF_PLANE=ECLIPTIC`、`REF_SYSTEM=J2000`、`VEC_CORR=NONE`、单位 km；以程序 TT JD 作为 HORIZONS 的 TDB 查询历元（TT/TDB 差异远小于模型误差）。
- 查询历元：TT JD `2461278.500873377`（民用 `2026-08-26 00:00 UTC`，ΔT ≈ 75.46 s）。HORIZONS 返回历元 `2461278.500873379`，差异可忽略。
- DE441 参考向量：
  - Earth（日心）：`[134021953.025, -69976595.382, 3573.101]` km
  - MoonGeo（地心）：`[240755.576, -316628.740, -13883.157]` km
- 程序输出：Earth `[134023571.059, -69983962.910, 4401.809]` km，MoonGeo `[240749.086, -316635.277, -13883.595]` km。

| 项目 | 程序 | DE441 | 误差 |
| --- | ---: | ---: | ---: |
| 地球日心位置误差 | | | 7,588.5 km（角误差 7.97″） |
| 月地矢量误差 | | | 9.2 km（角误差 4.73″） |
| 地日距离 | 151,195,478 km | 151,190,634 km | +4,844 km |
| 月地距离 | 398,008.5 km | 398,007.2 km | +1.3 km |

- 一致性：地球误差（约 7.6e3 km）与已记录 J2000（2,006 km）、2024-04-08（5,639 km）同为数千公里量级；月球误差（9.2 km）与已记录 5.0–5.9 km 同为公里量级。数值随历元在拟合窗口内波动，无趋势异常。

## 结论与边界

- 交接文档第 5 节全部回归项通过；当前日期的 DE441 对照缺口已补齐。
- 坐标合同：日心黄道 J2000，几何位置（无光行时/视位置修正），单位 km，时间尺度 TT。
- 误差来源：JPL SSD 近似根数 + 开普勒两体（地球）、Meeus 60+60 项（月球）、NASA/Espenak ΔT 近似（把 UT1 近似为 UTC，未引入 DUT1）。
- 当前实现仍不得称为 DE440/DE441 精密星历；本轮数字仅为独立误差对照。

## 遗留事项

- P0 浏览器控制台回归已可复现；若后续需要免浏览器脚本，可再评估（本轮用户决定不做 tests 目录）。
- P1 跨页面时间模块统一、P1 精度方案评估、P2 其他天体恢复与文案复核仍按交接文档优先级排队。
